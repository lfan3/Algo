/**
 * @param {string} s
 * @param {string} t
 * @return {string}
 * leetcode 76
 */
var minWindow = function(s, t) {
  const arr = t.split();
  const keys = new Set(arr)
  console.log('a', arr, keys)
};

// time out or not right

minWindow()